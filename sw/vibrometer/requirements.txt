# sw/vibrometer — analysis + acquisition deps.
# Live capture (capture.py --source sound) also needs PortAudio on the system.
numpy>=1.23
scipy>=1.9
matplotlib>=3.6
sounddevice>=0.4.6   # optional: only for live sound-card capture
pyserial>=3.5        # optional: only for the Teensy/RP2040 DAQ path
